# Mishka Den — the den mother for your family

A family hub web app: one place for the household calendar, chores, lists, and the everyday coordination a family needs. Built on the [karhu](https://github.com/bjornbasar/karhu) PHP microframework.

> *Mishka* is Russian for "little bear / cub". A **den** is where cubs live. A **den mother** (Cub Scouts) is the adult who keeps a troop of cubs organised. Mishka the app is the den mother for your family.

## Status

**v0.8.4** — Tracker Phase 5: offline logging + PWA shortcuts. Fifth and final release of the Tracker train (v0.8.0-v0.8.4). Family PWA on tablet + phone now works offline: log a meal / workout / weight while offline → payload queues into IndexedDB → auto-replays with a fresh CSRF token when connectivity returns (or on next page open). Plus three home-screen shortcuts on Android/Chromium install: Log Food · Log Exercise · Today. **No schema changes** — v0.6.13's `badge_awards` table + existing tracker tables cover everything; idempotency accepted at family scale (documented + escape hatch to `client_request_id UUID` if prod reveals doubles). **`App\Tracker\LoggedOnValidator`** (NEW, static-only) — shared parser now consumed by all three tracker POST controllers (`FoodLogController::store`, `ExerciseLogController::store`, `WeightController::store`). Contract: blank/null → `LocalDay::today` fallback (unchanged behaviour); non-blank must match `^\d{4}-\d{2}-\d{2}$` + parse to a real calendar date (rejects `2026-02-30` via format-round-trip); must be `<= household-local today` (future-reject); must be `>= today − 7 days` INCLUSIVE (past-cutoff — exactly 7 days ago accepted, 8 rejected). DST-safe via `->modify('-7 days')` on wall-clock midnight in $tz (steps calendar days, NOT 7·86400s) — docblock warns against a seconds-based refactor. WeightController's ad-hoc `measured_on` regex migrated to the shared validator (Plan-agent SHOULD-FIX #7 fold — symmetric behaviour across every user-suppliable-date endpoint). **JSON response path** on the same three POST controllers — `Accept: application/json` returns 200 `{status:'ok'}` on success, 400 `{status:'error', code:'validation', message}` on reject, 401 `{status:'error', code:'auth', message}` when session is gone. Load-bearing: `fetch()` follows redirects by default → a 302→`/login` returns 200 after auto-follow → without a JSON path, a queued POST replayed against an anonymous session would look like success and the queue row would silently disappear (Plan-agent BLOCKER #1 fold — DOCS #74). **`CsrfTokenController::show` extended contract**: response payload now carries `authenticated: bool` + `user_id: ?int` + `active_household_id: ?int` alongside the existing `token`. Powers the offline IIFE's session-scoping pre-check — never drain queue rows against the wrong session (Plan-agent BLOCKER #2/#3 fold). **`public/mishka-idb.js`** (NEW) — thin IndexedDB wrapper, zero deps. DB name `mishka-offline`, version 1. Two object stores: `queued_writes` (auto-inc key + compound index `by_session` on `[user_id, household_id]`; every row scoped to a session so shared iPads with mum→dad swaps never leak queued POSTs across users) + `library_cache` (household-scoped key `${hid}:${endpoint}:${q}` — offline live-search cache for `/health/log/food/search` + `/health/log/exercise/search`; 30-min TTL enforced at read time). `crypto.randomUUID()` with RFC-4122 v4 fallback for dev-mode over HTTP (Plan-agent UNKNOWN fold). **`public/mishka-offline.js`** (NEW) — offline logging IIFE. Reads three session meta tags (`mishka-user-id` / `mishka-household-id` / `mishka-household-tz`) from layout.twig; the `HAS_SESSION_CONTEXT` gate skips queue + flush on anonymous pages (Plan-agent BLOCKER #3 fold — DOMContentLoaded fires on `/login` / `/register` / `/offline` too). Form-submit interceptor on `[data-offline-queue]` marker fires CAPTURE-PHASE PRIORITY-FIRST — before v0.7.4's double-submit-prevention IIFE. When `!navigator.onLine`, `preventDefault()` + `stopPropagation()` + stash payload in IDB. Client-stamps `logged_on` via `new Date().toLocaleDateString('en-CA', {timeZone: hh_tz})` (household TZ, not device TZ). `flushQueue()` runs on `DOMContentLoaded` + `window.online`, guarded by module-scoped `flushInFlight` mutex to prevent concurrent double-drain. Per-row replay handles all response statuses: 200 → remove; 400 (validation reject) → hard-fail remove + console.warn; 401 (auth lost) → hold row; 403 (CSRF stale) → one-shot re-freshen + retry; 429/5xx (transient) → increment retry_count, cap at 5 then hard-fail; network error → increment retry. Exposes `window.MishkaOffline` for the layout.twig live-search IIFE's offline-fallback + write-through-cache hooks. **PWA shortcuts** (`public/manifest.webmanifest`) — three entries all using `/icon-192.png` (in PRECACHE): Log Food → `/health/log/food`; Log Exercise → `/health/log/exercise`; Today → `/health`. Android/Chromium show them on long-press of installed app icon; other browsers silently ignore. **SW precache 7 → 9** (adds `/mishka-idb.js` + `/mishka-offline.js`); `EXPECTED_PRECACHE` + count assertion in `ServiceWorkerStructureTest` bumped. **Layout meta tags** added inside `{% if session_email %}` head guard (correct Twig var per Plan-agent BLOCKER #4 fold — was `household.timezone` in draft, actual NavContext exposes `active_household.timezone`). `NavContext` extended to expose `session_user_id`. `<span data-offline-badge>` in the nav row shows `⚠ N queued` when non-empty. Layout's live-search IIFE gains offline `.catch` → `MishkaOffline.searchLibrary` + write-through `cacheLibraryResponse` on success. Three tracker form templates gain the `data-offline-queue` marker. **v0.8.2 privacy regression test's latent-bug fix**: was using `date('Y-m-d')` (UTC) which diverged from `TrackerController::today`'s `LocalDay::today('Pacific/Auckland')` during the ~10-hour daily UTC/NZ mismatch window. Cross-TZ CI passes silently pre-v0.8.4; the offline-work run caught it. Fixed by switching to `LocalDay::today` directly for axis-consistency with the widget's query. **Client-side JS tests**: none — mishka has no JS test runner (matches how `push-subscribe.js` v0.6.0 shipped). Manual Chrome DevTools smoke covers the client path. **Real-user smoke flow requires HTTPS** — the SW dev-hostname escape hatch skips fetch interception on `192.168.*` / localhost. Verified against `mishka.minified.work` post-deploy: offline-log a meal → queued → connectivity restored → auto-replays. `composer test` 996 / 2433 / 0, 1 skipped (was 971 / 2381 / 0 pre-v0.8.4 — +25 tests / +52 assertions across LoggedOnValidator + JSON path assertions on all 3 controllers + CsrfTokenController payload extension + ManifestTest shortcuts coverage + SW precache bump). PHPStan L6 clean per commit. **One round of Plan-agent ultrathink review** folded before implementation — 4 BLOCKERs + 8 SHOULD-FIXes + 4 NICE-TO-HAVEs + 5 UNKNOWNs. All BLOCKERs folded (auth-loss silent drop; cross-user leakage; anonymous-page bootstrap; wrong Twig var). All SHOULD-FIXes folded (5xx retry semantics; capture-phase priority-first vs v0.7.4; WeightController symmetric validator; JSON path on all three; precache count assertion; shortcut icons invariant test extension; RangeError meta-fallback; household-scoped library_cache). U1 + U5 resolved via file-reading during review (karhu `Request::header` lowercases via `strtolower()`; `URLSearchParams` + karhu `Request::post` proven working via push-subscribe.js precedent). U2/U3/U4 resolved via design (TrackerBadgeAwarder idempotent by DOCS #54 UNIQUE dedup; flushInFlight mutex covers `DOMContentLoaded` + `window.online` simultaneity; crypto.randomUUID fallback shim ships). See DOCS.md decision #74 + `docs/TRACKER.md` §13.

**v0.8.3** — Tracker Phase 4: household effort leaderboard + effort/consistency badges + streaks. Fourth release of the Tracker train. Reuses the Chores machinery (`BadgeAwardRepository`, `Achievements::computeDailyStreakLocal` new sibling walking household-local `Y-m-d` DATE strings, `WeekWindow`/`DayWindow` new local sibling helpers, `config/badges.php` extended with 9 new codes for 17 total) — no new schema tables (v0.6.13's `badge_awards` covers everything). New route `/health/leaderboard` mirroring `/badges` — dedicated page rather than embedding on `/health` (Today), keeping the PRIVATE (widget) and SHARED (leaderboard) surfaces cleanly separated per TRACKER-PLAN.md §5's privacy invariant. Ranks by weekly **MET-minutes** (from `exercise_log.met_minutes`, duration-branch only — matches v0.8.1's weight-independent formula `MET × minutes`); strength surfaces as a **session-count sidecar** (`(+N strength sessions this week)`) so contributors aren't invisible without violating v0.8.1's user-locked "no set-rep→minutes conversion" decision (DOCS #71). Filter axis is `logged_on DATE` household-local — new `WeekWindow::weekStartLocal / weekEndLocal / lookbackStartLocal` + `DayWindow::dayStartLocal / previousDayStartLocal / lookbackStartLocal` sibling helpers return `Y-m-d` strings for the DATE column, alongside the existing UTC-instant family for `chore_points_ledger.completed_at`. **New helper `App\Tracker\TrackerBadgeAwarder`** — separate class from `App\Chores\BadgeAwarder` per SRP (not a subclass; different SQL feeds, different code slugs). Fires from `ExerciseLogController::store` best-effort in the same try/catch shape as `ChoresController::handleDone` L320-346, with a defensive `?? 'Pacific/Auckland'` fallback for corrupted `households.timezone` rows. Evaluates 9 thresholds: `first_workout` / `ten_workouts` / `fifty_workouts` (COUNT); `five_hundred_met_minutes` / `five_thousand_met_minutes` (lifetime MET-min); `active_week` (first week ≥150 MET-min, WHO moderate-activity baseline); `four_week_effort_streak` (4 consecutive ISO weeks ≥ 150); `seven_day_ + thirty_day_activity_streak` (consecutive days with any workout). Weekly-effort streak uses new pure `computeWeeklyMetStreak` — buckets daily MET-min sums by PHP's ISO year-week (`format('o-\WW')`, portable + year-boundary-safe: 2027-01-01 → `2026-W53`). Daily-activity streak reuses `computeDailyStreakLocal` walking `Y-m-d` DATE strings — no TZ conversion, axis-consistent with the leaderboard. **New backfill CLI `bin/karhu tracker:badges-backfill`** — mirrors chore precedent (DOCS #54); iterates household × user, calls `TrackerBadgeAwarder::evaluateAndGrant` once per pair. Idempotent (BadgeAwardRepository UNIQUE dedup); safe to re-run. CI's deploy job gains a `Tracker badges backfill (idempotent)` step AFTER `Seed tracker exercises`, same failure-fails-deploy posture per DOCS #63 — so prod's pre-v0.8.3 exercise history retroactively earns the family's already-crossed thresholds on first-deploy without needing to log 50 more workouts. **Privacy invariant preserved from v0.8.2**: intake / weight / expenditure / net PRIVATE per user; only EFFORT (MET-minutes + strength session count + streaks + badges) shared with the household. Regression-tested by `TrackerLeaderboardControllerTest::test_leaderboard_does_not_leak_intake_or_weight_or_net` — response-body integration test with distinct-value fingerprinting (user B: 9876 kcal intake / 88.8 kg weight / 5432 kcal exercise; user A's leaderboard body must NOT contain any) PLUS shape-based negative assertions on `Intake:` / `Expenditure:` / `BMR:` / `Net:` (unique widget markers, absent from leaderboard) — defends against a future maintainer copy-pasting the Today balance widget into leaderboard.twig. **New per-user aggregation methods on `ExerciseLogRepository`**: `weeklyLeaderboardForHousehold` (LEFT JOIN roster + `u.id > 0` sentinel guard mirroring `ChoreRepository::leaderboardForHousehold` L360-389; `SUM(CASE WHEN exercise_type_snapshot = 'strength' THEN 1 ELSE 0 END)` for the sidecar; `MIN(m.joined_at)` tiebreak); `cumulativeStatsForUser` (single round trip covers count + total_met_minutes for awarder threshold checks); `recentLoggedOnsForHousehold` (batched roster-scoped DATE feed for leaderboard streaks — avoids N+1); `recentLoggedOnsForUser` (single-user variant for awarder); `dailyMetMinutesForUser` (per-day SUM for weekly-MET streak PHP-side bucketing — avoids `strftime`/`EXTRACT(week)` driver-portability). `composer test` 971 / 2381 / 0, 1 skipped (was 909 / 2273 / 0 pre-v0.8.3 — +62 tests / +108 assertions). PHPStan L6 clean per commit. `ExerciseLogRepositoryLeaderboardPgSmokeTest` proves NUMERIC(8,2) SUM roundtrip via PDO on PG16 + COALESCE(SUM(NULL), 0) semantics for strength-only user + household_members INNER JOIN dropping departed users + `u.id > 0` sentinel guard (SQLite skips the sentinel case — PG's sequence rejects users(id=0) insert, expected). Plan-agent ultrathink review folded before implementation — 9 SHOULD-FIX + 6 NICE-TO-HAVE + 3 UNKNOWNS: streak feed uses `logged_on` DATE (Plan-agent #1); batched `recentLoggedOnsForHousehold` avoids N+1 (#2); `week_entries` explicitly week-filtered as tiebreak (#3); one canonical `dailyMetMinutesForUser` method name (#4); `?? 'Pacific/Auckland'` TZ fallback (#5); `u.id > 0` sentinel guard mirroring chore precedent (#6); shape-based privacy assertions (#7); backfill CLI shipped (#8); code count 9 not 8 (#9). See DOCS.md decision #73 + `docs/TRACKER.md` §12.

**v0.8.2** — Tracker Phase 3: `tracker_profiles` + Mifflin-St Jeor BMR + Today energy-balance widget. Third release of the Tracker train. Ships one new table (`tracker_profiles` — one row per user, `PRIMARY KEY(user_id) REFERENCES users(id) ON DELETE CASCADE`, `sex CHECK IN ('male','female')`, `birth_year INTEGER` repo-bounded to `[1900, currentYear − 5]` per Plan-agent finding #5, `height_cm NUMERIC(5,1)` bounded `[50.0, 250.0]`, `base_activity NUMERIC(4,3)` bounded `[1.0, 2.5]`, with `updated_at` DEFAULT NOW handling the INSERT path so upserts write it only in the DO UPDATE branch), one static-only helper (`App\Tracker\BmrCalculator` — `calculate(sex, birthYear, heightCm, weightKg, nowYear=null)` returns null when any input is missing OR `age < 5` for fat-fingered birth-year defence-in-depth), one repository (`TrackerProfileRepository` — `findByUserId` / `upsert` with driver-portable `ON CONFLICT (user_id) DO UPDATE` mirroring three existing v0.6+ patterns in the codebase, all four fields validated at repo layer against `InvalidArgumentException` on out-of-range), two new per-user aggregation methods on existing repos (`FoodLogRepository::intakeKcalForUserDay(uid, hid, day)` + `ExerciseLogRepository::exerciseKcalForUserDay(uid, hid, day)` — strict user_id+household_id+day scoping so the widget's DB reads never touch other users' rows even in memory; COALESCE handles NULL kcal_snapshot on exercise rows), one new controller (`TrackerProfileController` — `GET /health/profile` shows form + BMR preview + latest weight; `POST /health/profile` upserts with 422 re-render on bounds fail; gates on `active_household_id` matching every `/health/*` route), `today.twig` extended with the energy-balance widget ABOVE meal sections + `[Profile]` header link alongside `[Foods] [Exercises] [Weight]`, and `profile_form.twig` (NEW) with `EXCLUDING WORKOUTS` copy on the base-activity SELECT + label text on each option ("Sedentary — desk job (1.20)" through "Very active — physical job (1.725)") + a paragraph reinforcer, defending against the double-count trap (Mifflin-St Jeor's textbook TDEE factor bakes exercise into the number, but v0.8.1 logs workouts separately in `exercise_log` — a user who picks 1.725 base_activity AND logs their gym time would double-count). Widget state fork: (1) profile missing → CTA to `/health/profile` (wins over weight-missing to avoid CTA loop for fresh users); (2) profile present but no weight → CTA to `/health/weight`; (3) both present → intake vs expenditure breakdown with net, showing `BMR + activity_delta + exercise` where `activity_delta = round(BMR × (base_activity − 1.0))` — Plan-agent finding #4 correction so the widget's math sums correctly (`total_expenditure = BMR + activity_delta + exercise = BMR × base_activity + exercise`) with `(base_activity X.XXX — excluding workouts)` inline note. **Privacy invariant**: intake / weight / expenditure / net are PRIVATE per user (per TRACKER-PLAN.md §5 — only effort will be shared with the household in v0.8.3's leaderboard). Regression-tested by a dedicated response-body integration test (`test_today_does_not_leak_other_users_intake_or_weight`) that fingerprints user B with three distinct values (9876 kcal intake / 88.8 kg weight / 5432 kcal exercise) and asserts NONE appear in user A's Today body — the twig template is the actual leak surface, not the repos. `composer test` 909 / 2273 / 0 (was 871 / 2196 / 0 pre-v0.8.2 — +38 tests / +77 assertions). PHPStan L6 clean per commit + `TrackerProfileRepositoryPgSmokeTest` proves the `ON CONFLICT (user_id) DO UPDATE` UPSERT + `NUMERIC(4,3)` roundtrip against real PG16 (v0.8.1.1 driver-portability discipline holds). One round of Plan-agent ultrathink folded before implementation: 8 findings (per-user aggregation methods vs household-wide reads; response-body privacy test spec vs repo-level scoping; widget state precedence; widget copy hides double-count trap for returning users AND example math didn't sum; birth_year fat-finger defence at repo+calculator both; `updated_at` INSERT/UPDATE split; `\App\` prefix in bootstrap.php per DOCS #55; NUMERIC(4,3) roundtrip proven in PG-smoke). See DOCS.md decision #72 + `docs/TRACKER.md` §11.

**v0.8.1** — Tracker Phase 2: exercise catalog + logging (duration + strength branches) + `weight_log`. Second release of the Tracker train. Ships three new tables (`exercises` with `type ∈ ('duration','strength')` discriminator + `met NUMERIC` bounded to `(0, 25]` + optional `default_rom_m` for compound lifts + partial UNIQUE on seed rows; `weight_log` per-user time series with `weight_kg` bounded to `[20.0, 300.0]`; `exercise_log` discriminated union with snapshot columns for name + type so deleted-exercise rows survive), one calculator (`ExerciseKcalCalculator` — split by branch, NO set-rep→minutes conversion per user-locked design decision), three repositories under `app/Tracker/`, three controllers (`ExerciseLogController` with bespoke live-search IIFE in `log_exercise.twig` because layout.twig's food-shaped IIFE couldn't be reused; `ExerciseCatalogController` mirroring `FoodLibraryController`; `WeightController` gated on `active_household_id` so household TZ drives `measured_on`), five new templates + today.twig extension with Exercise section + [Foods] [Exercises] [Weight] header links. `weight_log` was brought forward from v0.8.2 (per user's choice during planning) so kcal computation lands from day one — v0.8.2 correspondingly shrinks to `tracker_profiles` + BMR + Today energy-balance widget. New `bin/karhu tracker:seed-exercises` command with the same `{"version": 1, "exercises": [...]}` envelope shape as v0.8.0's foods seed; 36 exercises across Filipino-inclusive coverage (walking/running/cycling/hiking/swimming variants; tennis/basketball/badminton/soccer/dance/yoga; weight lifting/squats/bench press/deadlift/pull-ups/push-ups/plank/kettlebell/resistance bands; housework/gardening/dog walking) from the Compendium of Physical Activities 2024. CI's deploy job gains a `Seed tracker exercises` step AFTER `Seed tracker foods`. Discriminated-union kcal computation: duration branch computes `MET × 3.5 × weight_kg ÷ 200 × minutes` from `WeightLogRepository::latestForUser()` (NULL if never weighed in); strength branch uses `0.011723 × load_kg × default_rom_m × reps` (NULL when ROM undocumented) — weight-independent for strength, honest NULL fallbacks elsewhere. Snapshot columns (`exercise_name_snapshot` + `exercise_type_snapshot`) preserve context if the parent exercise is renamed or deleted — deliberate divergence from food_log's LEFT JOIN pattern documented in DOCS #71. `composer test` 871 / 2196 / 0 (was 816 / 2088 / 0 pre-v0.8.1 — +55 tests / +108 assertions). Two rounds of Plan-agent ultrathink review folded (CRITICAL: layout IIFE was food-shaped and couldn't be reused; MET bounds re-checked in `update()`; strength branch validation in controller AND repo; snapshot policy divergence justified; user-locked: no set-rep→minutes conversion, weight nav-only not Today-rendered, `[Foods] [Exercises] [Weight]` header links). See DOCS.md decision #71 + `docs/TRACKER.md` §10.

**v0.8.0** — Tracker Phase 1: dish library + serving-first food logging. First release of a 5-phase Tracker train (v0.8.0-v0.8.4 per `docs/ROADMAP.md`), UI-labelled **Health** in the nav. Ships three new tables (`foods` with `household_id NULL = global seed` rows, `food_servings` with a partial UNIQUE index on `is_default = TRUE`, `food_log` with `kcal_snapshot INTEGER` immutable at log time), three repositories (`FoodRepository` / `FoodServingRepository` / `FoodLogRepository`) under `app/Tracker/`, and a new `LocalDay::today(\DateTimeZone)` helper for `logged_on` (WeekWindow returns UTC ISO strings; the household-local calendar-date shape didn't exist). Three controllers wire up `/health` (Today, meal-grouped), `/health/log/food` (search + form + POST + delete), and `/health/foods` (browse + CRUD). A JSON `/health/log/food/search?q=…` endpoint uses `INNER JOIN food_servings … is_default = TRUE` so default-less dishes are dropped from search entirely (server-side null-guard); response is `Cache-Control: no-store` so the service worker doesn't cache it. A new `[data-live-search]`-marker inline IIFE in `layout.twig` (fourth after CSRF-refresh + double-submit-guard + hamburger toggle) debounces at 250ms, renders results via `textContent` (XSS-safe), and populates the enclosing form's hidden `food_id` + `serving_id` on click. New `bin/karhu tracker:seed-foods` command reads `db/seed/tracker_foods.json` (`{"version": 1, "foods": [...]}` envelope for forward-compat with v0.8.1's exercise seed), driver-portable idempotent `INSERT ... ON CONFLICT (name, source) WHERE household_id IS NULL DO NOTHING` (PG) / `INSERT OR IGNORE` (SQLite) — 41 dishes / 48 servings seeded across Filipino household staples (adobo, kare-kare, sinigang, tinola, menudo, afritada, sinangag, tapsilog, pancit, lumpia, lechon kawali, longganisa, tocino, champorado, arroz caldo), NZ everyday (kūmara, hoki, lamb chop, Weet-Bix, mince pie, flat white, Marmite/Vegemite on toast, pavlova, hokey pokey), and universals (white/brown rice, chicken breast, beef mince, eggs, milk, sourdough, banana, apple, coffee, water, roasted veg, green salad, yogurt). Each row carries `source ∈ ('philfct','nzfcd','usda','custom')` for provenance; kcal values are starting-point estimates from the cited public sources — editable in the Library UI per household without clearing the source column. CI's deploy job gains a `Seed tracker foods (idempotent)` step AFTER migrate; non-zero exit fails the deploy per DOCS #63 posture. `composer test` 816 / 2088 / 0 (was 766 / 1993 / 0 pre-v0.8.0 — +50 tests / +95 assertions). Two rounds of Plan-agent ultrathink review folded before implementation started (LocalDay helper discovery, intra-class route order, INNER JOIN not LEFT JOIN for search, driver-portable seed dedup, seed JSON version envelope, `updated_at` on UPDATE, empty-state UX copy, live-search contract test, `BOOLEAN` portability via SQL literals). See DOCS.md decision #70 + `docs/TRACKER.md` (living design doc).

**v0.7.7** — Family "stay logged in" — 30-day session lifetime. Closes DOCS #65 v0.7.4+ candidate. Two paired config bumps: (1) server-side `session.gc_maxlifetime` from PHP's 24-min default → 30 days (2,592,000 seconds), added to the existing `mishka-sessions.ini` in the Dockerfile alongside the v0.7.3 `save_path` pin — so the persistent-session-volume actually keeps files 30 days idle before GC. (2) Client-side cookie `Max-Age` from 0 (browser-session) → 30 days, via `new Session(['lifetime' => 2592000])` in `public/bootstrap.php`. Server GC threshold + browser cookie lifetime move together. Mental model: 30 days from LOGIN (or last `Session::regenerate()`), not "30 days from last activity" — `session_set_cookie_params` sets Max-Age at cookie-creation time, not per-request. Family-app pattern: users install as PWA on tablet, come back next day, still signed in. Safe because v0.7.0's `/me/sessions` UI lets any user revoke per-device (and password-change kicks all other sessions via `SessionRevocationGuard`). Local smoke verified: `ini_get('session.gc_maxlifetime')` → `2592000`; fresh `GET /login` sets `PHPSESSID=…; Max-Age=2592000; expires=<+30d>; path=/; HttpOnly; SameSite=Lax` (Secure is auto-detected on HTTPS in prod). `composer test` unchanged (766/1993/0 — infra-only, no PHP source changed except a ctor arg pass in bootstrap.php). See DOCS.md decision #69.

**v0.7.6** — Multi-stage docker image + `default_socket_timeout=5` ini pin. Two v0.7-era housekeeping items land together: (1) Dockerfile refactored from single-stage to `builder`/`runtime` two-stage split so the deployed image no longer carries `-dev` apt packages, `unzip`, `git`, or composer's `--prefer-dist` ZIP cache — closes DOCS #64 v0.7.3+ candidate; image size 625MB → 555MB (70MB / 11% shave, matches Plan-agent's realistic prediction). (2) New `mishka-socket-timeout.ini` pins PHP's `default_socket_timeout` from the language default (60s) down to 5s — closes DOCS #67 v0.7.6+ candidate. In practice this only affects Symfony Mailer's `EsmtpTransport` (via `stream_socket_client`); `libpq` (pdo_pgsql) uses direct BSD sockets and Guzzle/WebPush use `ext-curl` (`CURLOPT_TIMEOUT`), both of which ignore PHP's stream layer. So the 5s ini is effectively SMTP-only in scope. Also discovered during planning (Plan-agent finding folded): `pdo_sqlite` is statically compiled into `php:8.4-cli`, so `docker-php-ext-install pdo_sqlite` was a no-op AND the `libsqlite3-dev`/`libsqlite3-0` apt lines were dead weight — all three removed. Extensions in the new runtime image: `gmp`, `pdo_pgsql`, `pdo_sqlite` (static), `opcache`, `sodium`. `conf.d` has 6 files: 4 auto-generated docker-php-ext-* + `mishka-sessions.ini` (v0.7.3) + new `mishka-socket-timeout.ini`. `composer test` unchanged (766/1993/0 — infra-only, no PHP source touched). Verified locally end-to-end: `/login` returns 200, `/csrf-token` returns a fresh token, autoloader + `Composer\InstalledVersions::getVersion("twig/twig")` resolve. See DOCS.md decision #68.

**v0.7.5** — Real email delivery via Google Workspace SMTP relay. Mishka's Mailer has been fully implemented since v0.5 (five flows: verification, password reset, email change, email-change security alert, account-deleted courtesy) — but `MAILER_DSN` in prod pointed at MailHog on Ruxa (`smtp://192.168.4.9:1025`), a dev-only capture bin. No mishka email has ever reached a real inbox in prod. This release switches prod to Google Workspace's SMTP relay service (`smtp://smtp-relay.gmail.com:587?require_tls=1&local_domain=minified.work&timeout=5`) authenticated via IP-allowlist on the homelab's static IPv4 (`203.94.34.95`) — no App Password to rotate or leak. `MAIL_FROM_ADDRESS` moves from `noreply@mishka.minified.work` to `noreply@minified.work` (apex) so both SPF and DKIM align cleanly under DMARC without depending on Workspace's "allow subdomains" checkbox. SPF (`v=spf1 include:_spf.google.com ~all`) + DMARC (`v=DMARC1; p=none; rua=mailto:noreply@minified.work; adkim=r; aspf=r`) DNS records added on `minified.work`; DKIM was already live (`google._domainkey.minified.work`). New `bin/karhu mail:test --to=<email>` command smoke-tests outbound delivery post-deploy without going through /register + email verification — needs a new `Mailer::sendTest` public method plus explicit factory registration in `config/container.php` (karhu CLI uses a separate container from `public/bootstrap.php`). Workspace-repo compose change pins Ruxa's dev containers back at MailHog via `environment: MAILER_DSN` override so the decrypted prod .env can't accidentally leak real dev mail. Two rounds of adversarial plan review folded (critical: container.php missing Mailer factory would have crashed the CLI; blocker: TDD RED/GREEN split; several should-fix: local_domain in DSN, STDERR untestable in PHPUnit, --force-recreate on deploy, docblock coverage). Verified in dev: MailHog captured a test send end-to-end. See DOCS.md decision #67.

**v0.7.4** — Hotfix: client-side double-submit prevention (Android/PWA CSRF mismatch, part 2). v0.7.3 fixed the "container-recreate wipes /tmp sessions" class of CSRF-403s; a second, distinct class survived it. Post-v0.7.3 Ayula access logs showed a repeating pattern from Android Chrome (mobile + tablet PWA) only: `POST /login → 303` (successful login) followed **1 second later** by a second `POST /login → 403` (CSRF mismatch). Root cause: `AuthController::establishSession` rotates the session AND calls `Csrf::regenerate()` on success, so the token embedded in the pre-submit form HTML is dead immediately after the first POST completes. A native double-tap OR Android Chrome's autofill-re-trigger fires a second `submit` event carrying the stale token → `hash_equals` fails → 403. Windows Chrome (from the same live logs) never showed the follow-up 403 — Android-specific. Fix: single inline guard added to `templates/layout.twig` — a capture-phase `submit` listener sets `form.dataset.mishkaSubmitting = '1'`, synchronously disables all `type="submit"` buttons (which on modern browsers stops the second native click from ever emitting a submit event), and `preventDefault()`s any submit event that still gets through. Applies to EVERY form on the site; login was where it was visible (session/CSRF rotation makes the second POST 403), but every other form was at risk of silent double-writes on double-tap. Test count unchanged; no karhu or server-side change. SW_VERSION → `mishka-v0.7.4` per decision #51 always-bump. See DOCS.md decision #66.

**v0.7.3** — Hotfix: persistent PHP session storage (mobile/tablet CSRF mismatch). v0.7.2's move to self-contained-image exposed a latent issue: `session.save_path` was PHP's default `/tmp`, which is container-ephemeral. Every `docker compose up -d` (including the v0.7.2 deploy) wiped all session files. Mobile/tablet users with long-lived tabs holding a saved `PHPSESSID` cookie + stale CSRF token in the HTML then POST'd against an empty server-side session → karhu's `Csrf::getStoredToken()` returned `''` → `hash_equals` failed → 403. Fix: Dockerfile creates `/var/lib/mishka/sessions` + writes `/usr/local/etc/php/conf.d/mishka-sessions.ini` pinning `session.save_path` there. `docker-compose.yml` adds a named volume `mishka-sessions:/var/lib/mishka/sessions` on both `app` and `worker` services. Sessions now persist across container recreates → mobile clients with old PHPSESSID find their session on disk with the real `_csrf_token` → CSRF validates. Verified end-to-end locally: session file `sess_c7af93d…` written into the volume during a GET /login, still there after container removal. No bootstrap.php or middleware changes; pure infra fix.

**v0.7.2** — Self-contained docker image (no more bind-mount). Closes the v0.6.x architectural debt: `mishka-php:dev` was a thin PHP+extensions image with app code + vendor bind-mounted from the host (`/data/mishka:/app`). v0.7.2 bakes everything INTO the image via `composer install --no-dev` at build time + `COPY . .` + `composer dump-autoload --classmap-authoritative`. Compose drops the bind-mount. Deploy flow: new CI `build` job runs on Hurska self-hosted runner, SSHes Ruxa, runs `docker build` + smoke test (autoload + `Composer\InstalledVersions::getVersion("twig/twig")`) + push to local registry `192.168.4.9:5000/mishka-php:sha-<7chars>` + `:latest` (per main push) + `:vX.Y.Z` (per tag push); deploy job pulls `:latest` on Bosco, recreates containers (now image-only, no host source), runs migrate. Bosco's `/data/mishka/` shrinks from ~50MB to ~5KB (only `.env` + `docker-compose.yml` + `logs/`). Rollback: pin compose `image:` line to a `:sha-<old>` + `docker compose pull && up -d`. Image size 625MB (v0.7.3 multi-stage candidate could shave 100-200MB). Four rounds of adversarial plan review folded; local build + run validated HTTP 200 against the self-contained image.

**v0.7.1** — Auto-migrate on deploy + `schema_versions` audit. Closes the v0.7.0 ops gap where the manual `bin/karhu migrate` step was missed at deploy → prod 500'd on /login until run by hand. New `schema_versions` table (additive, append-only) records each migrate apply with `(schema_hash CHAR(64), applied_at, applied_by)`. `MigrateCommand` extended with SHA-256 hash-check: re-runs are cheap no-ops when the schema hasn't changed; differing hash applies + records a new row. Three flags: `--schema=path`, `--applied-by=name` (default 'manual'; CI passes 'ci-deploy'), `--force` (skip hash check). Exit codes: 0 = applied or no-op, 1 = failure, 2 = schema applied but audit-insert failed (partial-success; operator must investigate). New `deploy` job in `.github/workflows/ci.yml` runs on Hurska self-hosted runner after CI green: SSHes Ruxa (git pull + composer install --no-dev + sops decrypt + restore-dev-vendor), rsyncs source+vendor to Casal, runs `docker compose up -d`, then `docker exec mishka-app php ... migrate --applied-by=ci-deploy`. Concurrency block serializes rapid pushes; `$RUNNER_TEMP` keeps Slack notifications race-free; `.gitattributes` pins SQL files to LF for hash stability across runners. Three rounds of adversarial plan review folded.

**v0.7.0** — Per-device session revoke UI. First minor version since v0.6.0 (web push). Closes the v0.6+ roadmap item that's been pinned in this `## Status` line for many releases, and completes the account-hardening arc that started with v0.6.13's namespace-collision postmortem. New `/me/sessions` UI lists each device the user is signed in on (with "This device" badge on the current row) and lets them revoke any individual session via per-row button or bulk-revoke via "Revoke all other sessions". Built on a new `user_sessions` table (schema-additive — `id, user_id FK CASCADE, session_uuid CHAR(32) UNIQUE, user_agent, ip, created_at, last_used_at, revoked_at NULL`) keyed by an APP-LEVEL UUID (`bin2hex(random_bytes(16))`) — NOT PHP `session_id()`, because the PHP id rotates on `Session::regenerate()` (login + password change) while the app-level UUID survives across `$_SESSION` regenerate calls. The `SessionRevocationGuard` middleware extends to handle BOTH the existing v0.5.0 mass-revoke (auth_time vs password_changed_at) AND the new per-session revoke enforcement, with lazy-backfill so pre-v0.7.0 sessions appear in /me/sessions on their first post-deploy request. Login writes the UUID + INSERTs a row; logout marks the current session revoked (per user-confirmed scope: ONLY current, not all-device); password change additionally marks all OTHER sessions revoked for UI consistency. Three rounds of adversarial plan review folded into the design: round 1 dep-usage matrix, round 2 caught CRITICAL hoisting issue (lazy-backfill must run BEFORE all early-exit predicates) + `revokeAllForUserExcept(uid, 0)` defensive throw, round 3 caught clean-DTO boundary (UUID never enters rendered HTML), `Referrer-Policy: no-referrer` on /me/sessions, single findByUuid lookup.

**v0.6.20** — `AccountController` dep bundle refactor. Closes the v0.6.12-flagged 13-param ctor refactor (DOCS #53 follow-up; grew to 14 with v0.6.19's SystemRoleRepository). The 5 STRICTLY email-change-specific deps (`EmailChangeTokenRepository`, `EmailSendAttemptRepository`, `PasswordResetTokenRepository`, `EmailVerificationTokenRepository`, `UrlBuilder`) bundle into a new `final readonly` value-object `App\Account\AccountEmailFlow`. Controller ctor drops 14 → 9 params. Mailer + SystemRoleRepository stay at core (shared across 2+ flows); HouseholdRepository stays core (delete-only but lonely). All 11 internal callsites mechanically rewritten. Pure structural refactor — no behaviour change, test count unchanged at 733/1804/0. Sets up the per-flow bundling precedent for v0.7.0's session-revoke UI.

**v0.6.19** — Account-delete hardening + admin-promote UI. Closes two v0.6.13-candidate gaps flagged in DOCS #53. (1) **Admin-presence pre-check on self-delete**: if the user is the only `'admin'` in `system_roles`, the FK CASCADE would leave the system admin-less post-delete; v0.6.19 blocks this with a 422 + escape-hatch link to the new endpoint. (2) **`/me/admin/promote` endpoint** lets system admins grant the role to another user (promote-only semantics — the granter keeps their own admin until they actually delete, at which point the CASCADE removes it). New `App\Auth\SystemRoleRepository` owns all `system_roles` DB access (Karhu's `Rbac` is `final` + SQL-free per round-2 review); driver-aware idempotent INSERT mirrors `BadgeAwardRepository::grant` from decision #54. (3) **`user_deletions` audit table** (strictly additive, no PG_ONLY ALTER) writes `(user_id, deleted_at, household_ids)` INSIDE the existing delete transaction BEFORE the user DELETE — atomic audit. No FK to `users(id)` (the user row is gone post-cascade), mirroring `chore_points_ledger.credited_user_id` from decision #31. (4) **403 + dedicated forbidden page** is the new convention for system-admin-gated routes — reusable by v0.7+'s session-revoke UI. Nav link from `/me/profile` is gated on `SystemRoleRepository::isSystemAdmin`. AccountController now takes 14 ctor params; v0.6.20 will land the refactor flagged in DOCS #53.

**v0.6.18** — Worker bootstrap regression guard. Closes the DOCS #57 follow-up deferred from v0.6.16 (which guarded only the web `public/bootstrap.php`). The worker (mishka-worker container, running `php vendor/bjornbasar/karhu/bin/karhu push:worker`) has no single extractable bootstrap file — its wiring is split across bin/karhu (vendor), `config/container.php` (5 DI factories), `config/commands.php` (6-class registry), and `app/Commands/PushWorkerCommand.php` (3-dep ctor). New `tests/Smoke/WorkerBootstrapSmokeTest` replays what bin/karhu does up to but not including `dispatch()` (which would invoke `$worker->run()` and block) and asserts every command class auto-wires through the container without throwing. NOT `#[RunInSeparateProcess]` — the worker path doesn't register the karhu `ExceptionHandler`, so no process-global side effect exists to isolate. Reuses the v0.6.17 .env stub touch+unlink pattern + real-shape test-only VAPID keys from `BootstrapSmokeTest`. Also calls `$dispatcher->scanCommands($commands)` (round-2 defence in depth — catches broken `#[Command(...)]` attributes).

**v0.6.17** — Hotfix: BootstrapSmokeTest stub `.env` for GitHub-hosted CI. v0.6.16 shipped the smoke test verified green locally (the docker container bind-mounts `.env`) but the GitHub-hosted runner has no `.env` file (correctly — secrets aren't committed). `Dotenv::createImmutable(...)->safeLoad()` catches `InvalidPathException` for missing files, BUT the underlying `file_get_contents` emits an E_WARNING which the karhu `ExceptionHandler` (registered at `public/bootstrap.php:84`, immediately before the Dotenv call) promotes to `ErrorException` — bypassing safeLoad's catch. The smoke test now `touch`es an empty stub `.env` before the require if none exists, and `unlink`s it in `finally` if it created it. Pre-seeded `$_ENV` values win regardless (immutable semantics). No bootstrap code change.

**v0.6.16** — Bootstrap regression guards. Closes the DOCS #56 follow-up from the v0.6.15 hotfix. Two complementary guards: (1) `public/` added to `phpstan.neon.dist` analysis paths — Phase 1 verified that with `public/` in scope, PHPStan flags the v0.6.13-class namespace-collision bug (`Karhu\App\Chores\BadgeAwardRepository not found`) at analyse-time. (2) `public/index.php` extracted to `public/bootstrap.php` (returns the configured `Karhu\App` after all wiring); new `tests/Smoke/BootstrapSmokeTest` requires bootstrap.php in an isolated process (`#[RunInSeparateProcess]` — isolates `ExceptionHandler::register`'s process-global side effect) and asserts it returns a configured App with non-null router + container-resolved Connection. Real-shape test-only VAPID keys generated 2026-06-16 (WebPush::__construct calls VAPID::validate which asserts public-key strlen===65 bytes after base64url-decode; stub strings would throw at boot). `public/index.php` is now a 5-line dispatch shim: `$app = require __DIR__ . '/bootstrap.php'; $app->run();`. Also folded a pre-existing PHPStan OOM fix: `composer analyse` script gains `--memory-limit=512M` because PHPStan exceeds the container's default 128M (level 6 + vendor crawl). No schema, no behavioural change — internal refactor of the bootstrap + new test + PHPStan config additions.

**v0.6.15** — Hotfix: namespace-collision in `public/index.php`. The v0.6.13 wiring `new App\Chores\BadgeAwardRepository($db)` was a bare relative-name reference; combined with line 45's `use Karhu\App;` (aliasing `App` → `Karhu\App`), PHP resolved the lookup to `Karhu\App\Chores\BadgeAwardRepository` and 500'd on every request. PHPUnit + PHPStan + CI never executed `public/index.php` so the bug slipped through both v0.6.13 and v0.6.14 releases. Fix: added explicit `use App\Chores\BadgeAwardRepository;` + `use App\Chores\BadgeAwarder;` imports and stripped the `App\Chores\` prefix from the four references on lines 153/154/199/200. SW_VERSION bumped per decision #51 always-bump. No schema, no behavioural change — purely a namespace-resolution fix at the bootstrap layer.

**v0.6.14** — Daily streaks alongside weekly. Adds a parallel daily-streak track to the existing v0.4.3 weekly-streak machinery: consecutive days (household-tz midnight boundaries) with ≥1 chore completion. New `DayWindow` analogue to `WeekWindow` (DST-safe day arithmetic per decision #34); new `Achievements::computeDailyStreak` public static sibling to `computeStreak`. Live counter renders `📅 N` next to the existing `🔥 N` on the leaderboard at `daily_streak >= 2` (mirrors weekly's threshold). Two new badges via the v0.6.13 `badge_awards` table: `seven_day_streak` (🗓️ "Week strong") at 7 consecutive days and `thirty_day_streak` (📅 "Habit formed") at 30. Both eager-awarded via BadgeAwarder post-`markDone()`; both surface automatically on `/badges` via `config/badges.php` + the v0.6.13 grid + roster sections. No schema migration — `badge_awards.badge_code VARCHAR(64)` already accepts the new codes. No backfill for the streak badges (decision #54's `four_week_streak skip` precedent — eager-evaluation on the next chore-complete catches live streaks; households who streaked-then-stopped miss the badge, accepted rare case). `Achievements::compute()` return shape extends to `{badges, streak, daily_streak}`; `HomeController::achievementsBoard()`'s fallback default updated to match.

**v0.6.13** — Persistent badge history + `/badges` page. Reverses decision #35 (v0.4.3 "stateless badges, derived per render"). The 6 existing badges (first_chore, ten_chores, fifty_chores, centurion, five_hundred, four_week_streak) now persist in a new `badge_awards` table — `UNIQUE(household_id, user_id, badge_code)` for once-earned-forever semantics; `ON DELETE SET NULL` on `user_id` so badges survive account-delete as "Deleted user" rows (mirrors decision #31 + #53). Eager-award runs synchronously after `POST /chores/{id}/done` via a new `BadgeAwarder` service (best-effort — a badge-eval failure NEVER rolls back the chore-complete; `error_log` + continue). Streak rendering stays live (the 🔥 N number is still computed per-render via the v0.4.3 `Achievements::computeStreak` walk, now promoted to `public static`); only badge codes shift to persistent. New `/badges` page has two sections: per-user grid (6 cards, earned-vs-locked) + household roster (other members' badge counts + emoji row). Discoverability via `/me/profile`'s new "🏆 Your badges" link + clickable badge emoji on the `/chores` leaderboard. New `php bin/karhu badges:backfill` CLI walks `chore_points_ledger` once post-deploy to populate historical earnings with accurate `earned_at` (the triggering completed_at, not the deploy time); idempotent via ON CONFLICT DO NOTHING / INSERT OR IGNORE. `four_week_streak` is NOT backfilled (decision #14 — eager-evaluation on the next chore-complete catches live streaks). `ChoreRepository::markDone()` signature changed from `bool` to `?string` so BadgeAwarder reuses the same pinned timestamp without a re-SELECT (cascaded to 3 test files). Visit `/badges` from your profile, or click any badge emoji on the `/chores` leaderboard.

**v0.6.12** — Account deletion. Closes the v0.5.0 README deferral. POST `/me/delete` requires `current_password` re-auth (M1 always-verify) + a typed `confirm_email` (`hash_equals` against the canonical lowercased email — both inputs normalised with `strtolower`+`trim`). The three RESTRICT FKs that blocked the original implementation (`events.created_by`, `chores.created_by`, `chore_schedules.created_by`) migrate to NULLABLE + ON DELETE SET NULL via the PG_ONLY ALTER pattern (decision #47); authored content survives the author's account deletion as "Deleted user" (zero template changes — `created_by` is audit-only and never rendered). Owned-household pre-check blocks self-delete until the user transfers ownership or deletes each household first (mirrors decision #40 "owners cannot leave"). The single `DELETE FROM users WHERE id = :uid` inside one transaction fires the 12-table CASCADE chain + 7 SET NULL columns automatically. Session::destroy + explicit cookie clear post-delete; redirect to `/login?deleted=1` (query-param flash because the session is gone). Courtesy notification email to the user's email-on-file is fire-and-forget (detection signal for unauthorised deletion). Account delete is NOT a credential change — no `user_password_changes` write, no `Session::regenerate()` (regression-guarded by `test_post_delete_does_NOT_write_user_password_changes_row`).

**v0.6.11** — Email change. Closes the v0.5.0 deferral. Two-step flow: POST `/me/email` (with current-password re-auth + 3/10min/user rate limit) issues a 24h confirmation token sent to the NEW address; clicking the link renders a GET confirmation page (which defends against email-client link prefetching burning the token), and POSTing that page atomically swaps `users.email`, marks the new address verified, and invalidates pending `password_reset_tokens` + `email_verification_tokens` (mailbox-compromise hardening). An immediate notification to the OLD address surfaces the request (with the new email masked, no token, no cancel link — old-mailbox compromise must go through password-change for remediation). UNIQUE conflict on the swap (another user took the email mid-flow) surfaces as a 422 conflict page, never 500. Email change is NOT a credential change — no `user_password_changes` write, no `Session::regenerate()`. New `email_change_tokens` table mirrors v0.5.0's token shape; `email_send_attempts.kind` CHECK extended via the PG_ONLY ALTER pattern (decision #47).

**v0.6.10** — SW-version discipline correction. v0.6.7 introduced a `test_sw_version_matches_release` CI gate that asserts `SW_VERSION` in `public/service-worker.js` always matches the README `## Status` version. The v0.6.7 release-checklist wording, however, said to bump `SW_VERSION` only "if the release changes any precached asset" — a conditional rule that the test does NOT honour. v0.6.9 didn't touch any precached asset, didn't bump `SW_VERSION`, and CI flagged the gap. v0.6.10 syncs `SW_VERSION` to `mishka-v0.6.10`, tightens `docs/RELEASE.md` to "always bump on every release regardless of asset changes" (matches the test reality), and documents the corrected discipline as DOCS.md decision #51. No user-facing behavioural change other than the SW cache invalidating once on update — clients pick up no new assets because none changed since v0.6.9.

**v0.6.9** — Stuck-job recovery. The `mishka-worker` container's try/catch only catches handler exceptions; SIGKILL (OOM, host reboot, manual `docker kill`) leaves the job row stuck in `processing` forever. New `php bin/karhu jobs:unstick` command (cron-wired to `*/10 * * * *` on Ruxa) resets rows that have been processing for >5 min back to pending so the worker picks them up again. The UPDATE's WHERE clause is the dedup — a live worker that completes a row mid-cron flips status='completed' so the unstick row simply no longer matches; no race window. Closes the v0.6.0 plan's deferred `B9 — v0.6.1 candidate`. Bumps `bjornbasar/karhu-queue` to v0.2.0 (adds `QueueInterface::unstick` + bumps `updated_at` on every status transition + status-guards `complete()`/`fail()`). The push handler's existing dedup ledger means at-most-once recovers cleanly to at-least-once.

**v0.6.8** — Auto-refresh CSRF token on every page load. Tiny inline IIFE in `layout.twig` fetches `GET /csrf-token` on `DOMContentLoaded` (and on bfcache restore) and silently overwrites the in-page `<meta name="csrf-token">` content + every `input[name="_csrf_token"]` value with the live server-side token. Closes the cross-tab session-rotation gap from v0.6.7's known limitation — submitting a form from a tab that was open across a login/logout/password-change boundary no longer shows the plain-text "CSRF token mismatch" 403. Best-effort; offline pages keep their existing tokens. Also fixes a same-class bug in `push-subscribe.js` (the meta-tag read moved inside the click handler, was closure-captured at script-load time).

**v0.6.7** — PWA-grade service worker. The v0.6.0 push-only worker grew a versioned precache (app shell + manifest + icons + push-subscribe.js + a session-state-free /offline fallback), cache-first for static assets, network-first with a 3s timeout race for HTML pages, and silent skipWaiting+clients.claim updates — no "update available" banner, no mid-session reloads. Offline reads land; offline writes still need network (mishka does not queue POSTs). Bind-mount dev workflow protected by a hostname-based escape hatch. First release where `SW_VERSION` is a release-checklist line item.

**v0.6.6** — Creation-time push categories. Two new push kinds wired into the existing v0.6 stack: when a household member assigns you a chore, you get pushed at chore-creation time; when someone adds an event, every other household member gets pushed once at series-creation. Both default opt-in with per-user toggles on /me/notifications. Mishka's first explicit schema ALTER (PG-only fenced migration) — pre-v0.6.6 the schema was strictly additive via CREATE TABLE IF NOT EXISTS.

**v0.6.4** — Hamburger nav for narrow viewports. At 375px the top nav was overflowing by 217px (`<nav>` was 568px wide, body scrollWidth 592px). Mishka now ships a progressively-enhanced `<button aria-expanded>` hamburger that collapses the nav into a vertical drawer below 700px. ESC closes; viewport-cross resets state. No-JS users fall back to the pre-v0.6.4 behaviour — no lockout. First explicit ARIA disclosure pattern in mishka templates.

**v0.6.3** — PWA manifest + installability. Mishka is now a real installable Progressive Web App: Android Chrome offers an install affordance, and iOS 16.4+ Safari users get a true standalone PWA when they "Add to Home Screen" — which is what makes iOS Web Push actually fire. Existing bookmark-style installs need to be deleted and re-added to pick up the manifest.

**v0.6.0** — mobile polish + web push reminders. Mishka nudges family members on their phones when an event is about to start or chores have slipped past their due date. Web Push Protocol via VAPID, a dedicated worker container drains a karhu-queue, and the cron pushes happen at-most-once via a dedup ledger. Plus five `@media` rules in `layout.twig` close the worst of the 375px-viewport rough edges.

## Quick start

```bash
git clone https://github.com/bjornbasar/mishka
cd mishka
composer install
cp .env.example .env
$EDITOR .env                              # set DB_USER / DB_PASS for your PostgreSQL
php vendor/bjornbasar/karhu/bin/karhu migrate
composer serve                            # http://localhost:8080
```

Then open `http://localhost:8080/register`, create your account, and set up your household.

## Stack

- **Language:** PHP 8.4+
- **Framework:** [karhu ^0.1.1](https://github.com/bjornbasar/karhu) (HTTP, DI, middleware, auth primitives)
- **Database:** PostgreSQL via [karhu-db](https://github.com/bjornbasar/karhu-db)
- **Views:** Twig via [karhu-view](https://github.com/bjornbasar/karhu-view)
- **Passwords:** argon2id (via `Karhu\Auth\PasswordHasher`)
- **Tests:** PHPUnit 11 — SQLite in-memory for unit/integration + a PostgreSQL smoke job in CI for dialect-sensitive behavior
- **Static analysis:** PHPStan level 6

## What works in v0.6.0

Everything in v0.5.x plus:

- **Web push notifications** — opt-in per device via `/me/notifications`. Two notification types: event reminder (N min before, per-user; default 15) and a once-daily overdue-chore digest (07:30–08:30 in the household's timezone). At-most-once dedup. Click an event reminder → land on `/calendar`; click a digest → land on `/chores`.
- **Per-user preferences** — minutes-before slider, digest toggle. Defaults: 15 min + digest on.
- **Mobile polish** — agenda items stack date+title vertically, chore cards put the Done button on its own row (full-width, touch-tappable), the household switcher dropdown fits a 375px viewport, 48px touch-target floor.
- **🔔 nav icon** in the header points to `/me/notifications` for quick access.
- **CSRF token meta** — `<meta name="csrf-token">` in layout so JS can send `X-CSRF-Token` on the push subscribe POST.
- **VAPID boot guard** — mishka refuses to start without `VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` (RFC 8292 compliant).
- **`mishka-worker` container** — long-lived karhu-queue consumer; deployed under the `mishka` compose profile alongside MailHog.

## What works in v0.5.0

Carried forward from v0.1–v0.4.3: registration + login, households (N:M membership + invite codes), the full household calendar, per-user signed iCal feed, chores (one-off + recurring with round-robin/fixed assignment, overdue badges, durable points ledger, weekly + all-time leaderboard, pause/resume, per-chore rotation pools, kid-friendly badges + weekly streaks).

**New in v0.5.0:**
- **Email transport** — symfony/mailer (^7.2) wired in, MailHog dev compose, Postmark recommended for prod. `APP_URL` is required at boot (B1 — host-header injection in email links is impossible by construction).
- **Profile editing** — `/me/profile` lets a user edit their display name. Email change is deferred to v0.6+ (FK identifier; loud warning on /register).
- **Password change** — `/me/password` confirms the current password, validates the new one (12–128 chars, must differ), and rotates the session ID. The pinned-`$now` invariant (BL-2) ensures the user does NOT self-revoke.
- **Password reset** — anonymous `/password-reset` issues a 1h single-use token. Always-200 response + 1.5s timing floor + equalised miss-path work (defence vs. enumeration). Session is NOT auto-logged-in on success.
- **Email verification** — soft banner only ("Please verify your email — [Resend]"). 24h single-use token, sent at registration. Verifying flips the banner off immediately for the logged-in tab.
- **Session revocation** — `SessionRevocationGuard` middleware kicks any session whose `auth_time` predates the latest `user_password_changes.password_changed_at`. Pre-v0.5 sessions are grandfathered (decision U-1).
- **Household lifecycle** — owners can regenerate the invite code, transfer ownership atomically (`SELECT … FOR UPDATE` on PG; BL-3), or delete the household with typed-name confirmation (FK CASCADE wipes everything). Non-owners can leave; owners get a 422 directing them to transfer or delete first.
- **App-layer rate limit** — `email_send_attempts` caps `/password-reset` to 5/10min/IP and `/me/verify-email/resend` to 3/10min/user.

## Roadmap

- **v0.7+ candidates:** subscribe-to-external-calendar, Google sign-in (OAuth/OIDC).

## Docs

- [docs/USERGUIDE.md](docs/USERGUIDE.md) — the family-facing walkthrough (also served in-product at `/help`)
- [DOCS.md](DOCS.md) — full architecture, schema, routes, design decisions
- [karhu](https://github.com/bjornbasar/karhu) — the microframework this is built on

## License

MIT
